Cluttered Couch
===============

Generates "random" documents that you can then stuff into CouchDB. Especially
useful when building load tests when you don't care about what the data looks
like, just its size.
